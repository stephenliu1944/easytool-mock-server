var path = require('path');

module.exports = {
    port: 3000,
    // staticPath: path.resolve(__dirname, './test/static'),
    response: {
        headers: {
            'ABC': '1233333333333'
        }
    }
};